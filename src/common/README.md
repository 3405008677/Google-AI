## src/common 目录说明

## 概述
此目录存放跨模块可复用的“通用能力”，避免把与业务无关的基础设施逻辑散落在各个路由/服务里。

## 包含内容
- `prompts/`：提示词配置与线程安全的加载/热重载管理器。
- `function_calls/`：工具（function calling）Schema 配置、注册、发现与执行封装。

## 使用建议
- 业务模块（如 `router/agents/`）尽量只“调用”这里提供的能力，而不要把通用逻辑复制过去。
